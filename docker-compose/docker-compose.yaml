version: "3.3"

services:
  db:
    image: mysql:5.7
    volumes:
      - ~/docker-volumes/mysql:/var/lib/mysql
    restart: always
    ports:
      - 3306:3306
    env_file: ./mysqldb.env
    container_name: mysqldb
